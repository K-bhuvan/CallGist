"""Audio transcription (Milestone 1 stub).

Planned implementation: faster-whisper local model (medium on CPU)
to transcribe uploaded audio without an API key. Diarization is out of scope.
"""


def transcribe_audio(audio_path: str) -> str:
    raise NotImplementedError(
        "Audio transcription will use faster-whisper in a later milestone."
    )
